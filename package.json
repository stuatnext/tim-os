{
  "name": "tim-os",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Tim OS — a Claude Code routine that does brand-intelligence work for Timothy Oh (COL Group / ReelShort / FlareFlow) in-session, then renders a static HTML dashboard served from GitHub Pages.",
  "scripts": {
    "init": "tsx scripts/init.ts",
    "feeds": "tsx scripts/fetch-feeds.ts",
    "render": "tsx scripts/render-only.ts"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
